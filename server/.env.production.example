# Production Environment Configuration for Render.com
# This template is specifically configured for Render.com deployment

# Server Configuration
NODE_ENV=production
PORT=10000

# Database Configuration
# IMPORTANT: Use Render's Internal Database URL from your MySQL instance
# Format: mysql://user:password@hostname:port/database
DATABASE_URL=mysql://user:password@dpg-xxxxxxxxxxxx-a.ohio-postgres.render.com:3306/devine_water
DB_SSL=true

# JWT Authentication
# CRITICAL: Use a strong, unique secret!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_production_jwt_secret_minimum_32_characters_here

# CORS Configuration
# Add your GitHub Pages URL and any custom domains
# Format: comma-separated list, no spaces
ALLOW_ORIGINS=https://yourusername.github.io,https://your-custom-domain.com

# Database Connection Pool (optional tuning)
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0

# Rate Limiting (already configured in code, but can override)
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# AUTH_RATE_LIMIT_MAX=5

# Render.com Specific
# These are automatically set by Render, but documented here for reference:
# RENDER=true
# RENDER_SERVICE_NAME=your-service-name
# RENDER_INSTANCE_ID=srv-xxxxx

# Health Check
# Render will automatically ping /healthz endpoint

# Deployment Checklist:
# ✅ JWT_SECRET is at least 32 characters
# ✅ DATABASE_URL points to Internal Database URL
# ✅ DB_SSL=true is set
# ✅ ALLOW_ORIGINS includes your actual frontend URL(s)
# ✅ Change admin password after first deployment
# ✅ Test all endpoints after deployment
# ✅ Monitor logs for errors
