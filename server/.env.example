# Devine Water Backend - Environment Configuration
# Copy this file to .env and configure with your actual values

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (Local Development)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_local_mysql_password_here
DB_NAME=devine_water

# For Render.com MySQL or other cloud providers, use connection URL:
# DATABASE_URL=mysql://user:password@host:port/database
# DB_SSL=true

# Database Connection Pool (optional, defaults applied in config.js)
# DB_CONNECTION_LIMIT=10
# DB_QUEUE_LIMIT=0

# JWT Authentication
# CRITICAL: Generate a strong secret for production!
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_strong_jwt_secret_min_32_characters_here

# CORS Configuration
# For local development:
ALLOW_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# For production with GitHub Pages:
# ALLOW_ORIGINS=https://ashhar10.github.io,https://your-custom-domain.com

# Security Notes:
# 1. NEVER commit the actual .env file to version control
# 2. Use strong, unique passwords (min 12 characters)
# 3. JWT_SECRET must be at least 32 characters in production
# 4. Enable DB_SSL=true for production databases
# 5. ALLOW_ORIGINS should list only trusted domains (no wildcards in production)